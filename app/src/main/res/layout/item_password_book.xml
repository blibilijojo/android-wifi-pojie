<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    android:background="@drawable/rounded_corner"
    android:layout_marginBottom="8dp">

    <!-- 密码本名称 -->
    <TextView
        android:id="@+id/password_book_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="密码本名称"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@+id/delete_password_book_btn"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 密码本信息 -->
    <TextView
        android:id="@+id/password_book_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="密码数量: 0 | 创建时间: 2023-01-01 00:00"
        android:textSize="14sp"
        android:textColor="@color/secondary_text"
        android:layout_marginTop="4dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@+id/delete_password_book_btn"
        app:layout_constraintTop_toBottomOf="@+id/password_book_name" />

    <!-- 删除按钮 -->
    <Button
        android:id="@+id/delete_password_book_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="删除"
        android:textColor="@color/red"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="@+id/password_book_info" />

</androidx.constraintlayout.widget.ConstraintLayout>